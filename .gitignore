# Worktrees
.worktrees/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# env files (.env, .env.prod, .env.local, etc) — but keep .env.example committed
.env
.env.*
!.env.example


# pycache
__pycache__/
*.pyc
*.pyo
*.pyd

# superpowers brainstorm sessions / plans / specs (local-only, not pushed to GitHub)
docs/superpowers/plans/
docs/superpowers/specs/

# supabase temp files
.temp

# git worktrees
.worktrees/

# bun lockfile (not used — project uses npm)
bun.lockb

# PostHog dashboard setup state (entity ID maps, env-scoped)
src/backend/scripts/.posthog_dashboard_state.*.json

# Test reports (generated by pytest --junitxml in CI)
junit.xml

# go-task runner cache (source-fingerprint checksums for up-to-date checks)
.task/