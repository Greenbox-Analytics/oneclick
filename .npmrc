; The dependency tree has a pre-existing peer conflict (tailwindcss v4 is
; required by @blocknote/shadcn while other tooling pins older ranges). Installing
; with legacy peer resolution keeps `npm install` / `npm ci` reproducible.
legacy-peer-deps=true
